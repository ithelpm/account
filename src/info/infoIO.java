package info;

import java.io.*;

class indoIO
{
    public static void IO(String path)
    {
        File file = new File(path);
        if (file.isFile() && file.exists())
        {
            
        }
    }
}